{
  "name": "wehave-ai",
  "version": "1.0.0",
  "description": "WeHave.ai - AI-powered business card generator",
  "scripts": {
    "install:all": "yarn install && cd backend && yarn install && cd ../frontend && yarn install",
    "build": "cd backend && yarn install && yarn build && cd ../frontend && yarn install && yarn build",
    "start": "cd backend && yarn install && yarn build && yarn start",
    "dev": "concurrently \"cd backend && yarn dev\" \"cd frontend && yarn dev\""
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 